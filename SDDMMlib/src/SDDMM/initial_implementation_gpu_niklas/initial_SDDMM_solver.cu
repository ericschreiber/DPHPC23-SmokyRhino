#include <cuda_runtime.h>

#include <iostream>
#include <random>

#include "utils.h"

void compute(
    n_rows,
    m,
    n,
    k,
    matrixA_GPU,
    matrixB_GPU,
    values_matrixC_GPU,
    values_matrixResult_GPU,
    colIndices_matrixResult_GPU,
    rowPtr_matrixResult_GPU)
{
    // TO-DO
    m = 10;
}